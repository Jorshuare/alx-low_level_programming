#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - print single digit numbers
 *
 * Description: print single digit numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char c = '0';


	while (i <= '9')
	{
		putchar(i);
		i++;
	}
	putchar('\);


	return (0);
}
